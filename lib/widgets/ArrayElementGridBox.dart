import 'package:flutter/material.dart';

class ArrayElementGridBox extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      child: Container(
        color: Colors.blue,
      ),
    );
  }
}
