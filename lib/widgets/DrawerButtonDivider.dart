import 'package:flutter/material.dart';

class DrawerButtonDivider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Divider(
      indent: 50,
      endIndent: 50,
      thickness: 0.2,
      color: Colors.black,
    );
  }
}
